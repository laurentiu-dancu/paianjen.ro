{
  "name": "paianjen-assets",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "deploy": "cd .. && MIX_ENV=prod mix assets.deploy && rm -f _build/esbuild*"
  },
  "dependencies": {
    "htmx.org": "^2.0.0"
  }
}
